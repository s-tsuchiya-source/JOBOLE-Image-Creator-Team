# New Order Workflow

## 目的
新規受注時に案件の基本情報を整理し、Google Drive側に案件フォルダを作成する。

## 流れ
1. 受注情報を確認する
2. 案件IDを採番する
3. `project.yaml` を作成する
4. `creative-manifest.csv` の初期データを作成する
5. 案件ディレクトリをGoogle Drive上に生成する
6. Production Directorへ引き渡す
